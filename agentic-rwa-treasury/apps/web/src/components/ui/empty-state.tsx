import type { ReactNode } from "react";
import { Typography } from "./typography";
export function EmptyState({ icon, title, description, action }: { icon?: ReactNode; title: ReactNode; description: ReactNode; action?: ReactNode }) { return <div className="grid min-h-52 place-items-center px-6 text-center"><div className="max-w-sm"><div className="mx-auto mb-3 grid size-8 place-items-center rounded-[0.625rem] bg-soft text-muted">{icon}</div><Typography as="h3" className="text-ink" variant="title">{title}</Typography><Typography className="mx-auto mt-1 text-subtle" variant="body">{description}</Typography>{action ? <div className="mt-4">{action}</div> : null}</div></div>; }
